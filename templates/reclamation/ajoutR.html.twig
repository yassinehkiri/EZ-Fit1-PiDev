{% extends 'front.html.twig' %}

{% block title %}Ajouter Reclamation{% endblock %}
{% block body %}
    <div class="fox">
    <center>
    {{ form_start(f) }}
    {{ form_widget(f) }}
    {{ form_end(f) }}
    </center>
    </div>
{% endblock %}